using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class drinkWater : MonoBehaviour
{
    /*// Start is called before the first frame update
    public AudioSource drinking;
    private bool isColliding = false;
    
    public Text drinkText;

    private void Awake()
    {
        
    }
    void Update()
    {
        if(Input.GetKeyDown(KeyCode.E) && isColliding == true)
        {
            if (WaterVase.water > 50f)
            {
                drinking.Play(0);
                WaterVase.water = 0;

            }
            
        }    
        
    }


    
    private void OnTriggerStay(Collider other)
    {
        if (other.CompareTag("Player"))
        {
            isColliding = true;
            Debug.Log(WaterVase.water);
            if (WaterVase.water > 50f)
            {
                drinkText.text = "Press E to drink";

            }

        }
    }
    private void OnTriggerExit(Collider other)
    {
        if (other.CompareTag("Player"))
        {
            isColliding = false;
            drinkText.text = "";

        }
    }*/
}
